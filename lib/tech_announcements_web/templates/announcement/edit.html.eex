<h2>Edit Announcement</h2>

<%= render "form.html", Map.put(assigns, :action, announcement_path(@conn, :update, @announcement)) %>

<span><%= link "Back", to: announcement_path(@conn, :index) %></span>
